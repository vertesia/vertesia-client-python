# coding: utf-8

"""
    Vertesia API

    The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.

    The version of the OpenAPI document: 1.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from vertesia_client.openapi.models.activity_fetch_spec import ActivityFetchSpec
from vertesia_client.openapi.models.dsl_activity_options import DSLActivityOptions
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DSLActivityStep(BaseModel):
    """
    DSLActivityStep
    """ # noqa: E501
    type: StrictStr = Field(description="The type fo the step. If not set defaults to \"activity\"")
    name: StrictStr = Field(description="The name of the activity function")
    title: Optional[StrictStr] = Field(default=None, description="Title of the activity to be displayed in the UI workflow builder")
    description: Optional[StrictStr] = Field(default=None, description="The description of the activity to e displayed in the UI workflow builder")
    params: Optional[Dict[str, Any]] = Field(default=None, description="Activities parameters. These parameters can be either literals (hardcoded strings, numbers, booleans, objects, arrays etc.), either references to the workflow variables. The workflow variables are built from the workflow params (e.g. the workflow configuration) and from the result of the previous activities.")
    output: Optional[StrictStr] = Field(default=None, description="The name of the workflow variable that will store the result of the activity If not specified the result will not be stored The parameters describe how the actual parameters will be obtained from the workflow execution vars. since it may contain references to workflow execution vars.")
    condition: Optional[Dict[str, Any]] = Field(default=None, description="A JSON expression which evaluate to true or false similar to mongo matches. We support for now basic expression like: $true, $false, $eq, $ne, $gt, $gte, $lt, $lte, $in, $nin, $regexp {$eq: {name: value}}, Ex: {$eq: {wfVarName: value}}")
    var_import: Optional[List[Any]] = Field(default=None, description="The import spec is used to import data from workflow variables. The import spec is a list of variable names to import from the workflow context. You can also use objects to rename the imported variables, or to reference an expression. Example: [\"runId\", {\"typeId\": \"docType.id\"}]", alias="import")
    fetch: Optional[Dict[str, ActivityFetchSpec]] = Field(default=None, description="The fetch phase is used to fetch data from external sources.")
    projection: Optional[Dict[str, Any]] = Field(default=None, description="Projection to apply to the result. Not all activities support this.")
    parallel: Optional[StrictBool] = Field(default=None, description="If true the activity will be executed in parallel with the other activities. (i.e. the workflow will not wait for the activity to finish before starting the next one)")
    var_await: Optional[StrictStr] = Field(default=None, description="Await for a parallel activity execution to return.", alias="await")
    options: Optional[DSLActivityOptions] = Field(default=None, description="Activity options for configuring the activity execution, which overrides the activity options defined at workflow level.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["type", "name", "title", "description", "params", "output", "condition", "import", "fetch", "projection", "parallel", "await", "options"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DSLActivityStep from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in fetch (dict)
        _field_dict = {}
        if self.fetch:
            for _key_fetch in self.fetch:
                if self.fetch[_key_fetch]:
                    _field_dict[_key_fetch] = self.fetch[_key_fetch].to_dict()
            _dict['fetch'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of options
        if self.options:
            _dict['options'] = self.options.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DSLActivityStep from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "name": obj.get("name"),
            "title": obj.get("title"),
            "description": obj.get("description"),
            "params": obj.get("params"),
            "output": obj.get("output"),
            "condition": obj.get("condition"),
            "import": obj.get("import"),
            "fetch": dict(
                (_k, ActivityFetchSpec.from_dict(_v))
                for _k, _v in obj["fetch"].items()
            )
            if obj.get("fetch") is not None
            else None,
            "projection": obj.get("projection"),
            "parallel": obj.get("parallel"),
            "await": obj.get("await"),
            "options": DSLActivityOptions.from_dict(obj["options"]) if obj.get("options") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


