# coding: utf-8

"""
    Vertesia API

    The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.

    The version of the OpenAPI document: 1.5.0-dev.20260714.072725Z
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt
from typing import Any, ClassVar, Dict, Optional, Union
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ProjectIntakeSniffConfiguration(BaseModel):
    """
    Fast pre-conversion type identification (the \"sniff\") for untyped documents. The sniff classifies a document from cheap local evidence (first/last page text, a low-res first-page image, office docProps) BEFORE any conversion, so a high-confidence match can apply the type's intake policy — including skipping conversion — without paying for it first.
    """ # noqa: E501
    enabled: Optional[StrictBool] = Field(default=None, description="Enable the pre-conversion sniff for untyped documents. Defaults to true. Can be overridden per run with the `sniffEnabled` workflow var.")
    high_confidence: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Confidence at or above which the sniffed type is committed and its full policy applied (including conversion-skip). 0..1, defaults to 0.85.")
    medium_confidence: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Confidence at or above which the sniffed type is treated as provisional: the document still converts and the post-conversion selector confirms on neutral evidence. 0..1, defaults to 0.6. Below this the sniff result is advisory provenance only.")
    min_pages: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Minimum page count for the sniff LLM call. Below this, conversion is cheap and full converted text is better selection evidence, so intake uses the standard convert-then-select path. Documents with unknown page counts are sniffed. Defaults to 5; 0 means always sniff.")
    __properties: ClassVar[List[str]] = ["enabled", "high_confidence", "medium_confidence", "min_pages"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectIntakeSniffConfiguration from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectIntakeSniffConfiguration from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "enabled": obj.get("enabled"),
            "high_confidence": obj.get("high_confidence"),
            "medium_confidence": obj.get("medium_confidence"),
            "min_pages": obj.get("min_pages")
        })
        return _obj


