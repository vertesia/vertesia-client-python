# coding: utf-8

"""
    Vertesia API

    The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.

    The version of the OpenAPI document: 1.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MCPOAuthConfig(BaseModel):
    """
    Install-time OAuth provisioning blueprint for an MCP collection. Defines how to auto-create an OAuth provider when the app is installed. Does NOT affect runtime behaviour — the runtime uses oauth_bindings on AppInstallation.
    """ # noqa: E501
    name: Optional[StrictStr] = Field(default=None, description="Name for the OAuth provider to create at install time. Defaults to the collection id converted to kebab-case if not specified.")
    display_name: Optional[StrictStr] = Field(default=None, description="Human-readable display name for the created OAuth provider.")
    grant_type: Optional[StrictStr] = None
    authorization_endpoint: Optional[StrictStr] = None
    token_endpoint: Optional[StrictStr] = None
    revocation_endpoint: Optional[StrictStr] = None
    client_id: Optional[StrictStr] = Field(default=None, description="Pre-configured client_id. Omit if the installer must supply it (include 'client_id' in required_at_install).")
    use_pkce: Optional[StrictBool] = None
    default_scopes: Optional[List[StrictStr]] = None
    required_at_install: Optional[List[StrictStr]] = Field(default=None, description="Parameters the installer must provide at install time. These are shown as form fields in composable-ui before the install completes. - 'client_id': user supplies the OAuth client ID - 'client_secret': user supplies the OAuth client secret")
    __properties: ClassVar[List[str]] = ["name", "display_name", "grant_type", "authorization_endpoint", "token_endpoint", "revocation_endpoint", "client_id", "use_pkce", "default_scopes", "required_at_install"]

    @field_validator('grant_type')
    def grant_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        return value

    @field_validator('required_at_install')
    def required_at_install_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['client_id', 'client_secret', 'scopes']):
                raise ValueError("each list item must be one of ('client_id', 'client_secret', 'scopes')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MCPOAuthConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MCPOAuthConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "display_name": obj.get("display_name"),
            "grant_type": obj.get("grant_type"),
            "authorization_endpoint": obj.get("authorization_endpoint"),
            "token_endpoint": obj.get("token_endpoint"),
            "revocation_endpoint": obj.get("revocation_endpoint"),
            "client_id": obj.get("client_id"),
            "use_pkce": obj.get("use_pkce"),
            "default_scopes": obj.get("default_scopes"),
            "required_at_install": obj.get("required_at_install")
        })
        return _obj


